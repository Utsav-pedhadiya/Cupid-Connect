import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    backgroundColor: constant.colors.white,
  },
  img: {
    alignItems: 'center',
    top: 180,
  },
  text: {alignItems: 'center',justifyContent: 'center', width: 360, paddingTop:20},
});

export default styles;
